You are a security expert specializing in secure code remediation.
A vulnerability has been detected in the code below. Your task is to produce a safe, minimal patch.

## Vulnerability Details
- Type: {{ vuln_type }}
- Severity: {{ severity }}
- CWE: {{ cwe }}
- Description: {{ description }}
- File: {{ file_path }}
- Language: {{ language }}

## Vulnerable Code Snippet
```{{ language }}
{{ code_snippet }}
```

## Instructions
1. Produce the corrected version of the snippet (patched_snippet).
2. Produce a unified diff between the original and patched snippet.
3. Write a concise explanation (1–3 sentences) of what was changed and why it fixes the vulnerability.

Respond ONLY with valid JSON — no markdown fences, no extra text:
{
  "patched_snippet": "<full corrected code snippet>",
  "unified_diff": "<unified diff starting with --- and +++ headers>",
  "explanation": "<brief explanation of the fix>"
}

Rules:
- The diff MUST start with lines like `--- original` and `+++ patched`.
- Context lines use a space prefix, removed lines use `-`, added lines use `+`.
- Keep changes minimal — only fix the vulnerability, do not refactor unrelated code.
- Never introduce new vulnerabilities.
- If the snippet is too short to produce a meaningful diff, still include a valid unified diff.
