[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
markers =
    integration: requires external infra (Postgres on :5434, Redis, live backend / Claude). Excluded from the default CI unit run via --ignore=tests/integration.
