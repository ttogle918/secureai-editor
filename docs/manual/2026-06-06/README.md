# Pagori (SecureAI) 사용자 매뉴얼 — 2026-06-06

배포 시 사용자에게 보여줄 수 있는 **인터랙티브 기능 안내 매뉴얼**입니다.

## 여는 법
`index.html`을 브라우저로 엽니다 (더블클릭 또는 `file:///.../docs/manual/2026-06-06/index.html`).
별도 서버·빌드 불필요 — 단일 HTML(인라인 CSS/JS, 외부 의존성 0).

## 구성
- **좌측 사이드바**: 17개 기능을 카테고리(계정·시작하기·핵심기능·스캔·리포트·협업·설정)로 분류.
- **단계 진행**: 각 기능을 단계별로 안내. 하단 `이전 / 다음` 버튼 또는 키보드 `←` `→`.
- **빨간 점선 박스**: 그 단계에서 **클릭해야 할 위치**를 가리킴 (`👆 힌트` 말풍선 포함).
- **진행 인디케이터**: 상단에 `기능 N/17 · 단계 N/M` + 전체 진행 막대, 하단에 단계 점.
- **안내 박스**: 규칙(주황)·팁(파랑)·주의(빨강) 색으로 구분.

## 포함 기능 (17)
회원가입 → 이메일 인증 → 로그인(이메일/GitHub) → 온보딩(워크스페이스 모드) →
코드 분석(에디터) → 취약점 트리아지/오탐 → AI 보안 채팅 → 스캔 모드(Audit/Pipeline) →
GitHub 레포 스캔 → 커밋 시크릿 스캔 → Compliance 리포트 → SBOM/CVE →
대시보드 & ROI PDF → 팀/조직 & 멤버 초대 → 설정(API키·모델·스캔) → 프로필 → 로그아웃.

## 비고
- 화면은 실제 앱 디자인 토큰(Pagori Dark OLED + Orange `#f97316`, severity 색상)으로 만든 **충실 목업**입니다. 브라우저 자동화 캡처 도구가 없어 실 스크린샷 대신 목업으로 구성했습니다 — 실제 라우트(`/login`, `/editor`, `/team/{slug}/members` 등)와 동선은 코드 기준으로 일치시켰습니다.
- 팀 멤버 초대 안내(14번)는 2026-06-06 수정된 BUG-1105-2(owner/admin role 게이팅) 반영.
</content>
