# 리팩토링 우선순위 로드맵
> 기준일: 2026-05-16  
> 참조: `20260516_stage3.md` — 설계 원칙 위반 대조표  
> 원칙: 기능 동작을 깨지 않으면서 설계 원칙을 회복한다. 한 번에 하나의 레이어만 건드린다.

---

## 판단 기준

| 기준 | 설명 |
|------|------|
| **영향 범위** | 위반이 몇 개 파일/도메인에 걸쳐 있는가 |
| **위험도** | 버그·보안 취약점으로 직결되는가 |
| **역행 비용** | 나중에 고칠수록 얼마나 더 어려워지는가 |
| **철학 문서 명시 여부** | `05_ARCHITECTURE_PHILOSOPHY.md`가 명시적으로 금지한 패턴인가 |

---

## Tier 0 — 즉시 수정 (버그·보안 직결)

> 기능이 잘못 동작하거나 보안 허점이 있는 항목. 리팩토링이 아니라 버그 수정.

### R-00-A. OrganizationService 이메일 발송 미연동

| 항목 | 내용 |
|------|------|
| **파일** | `domain/organization/service/OrganizationService.java:209` |
| **위반 원칙** | P7 (기능 미완성) |
| **문제** | `inviteByEmail()` 내부에 TODO 주석만 있음. 초대 이메일이 실제로 발송되지 않아 초대 기능 전체가 UX 단절 상태 |
| **수정 내용** | `EmailService.sendOrgInvitation(email, token, orgName)` 메서드 추가 후 호출 연결 |
| **영향 파일** | `EmailService.java`, `OrganizationService.java` |
| **예상 작업량** | S (반나절) |
| **테스트** | 초대 이메일 실제 수신 확인 + 토큰 만료(72h) 검증 |

### R-00-B. DastController X-Forwarded-For 무조건 신뢰

| 항목 | 내용 |
|------|------|
| **파일** | `domain/dast/controller/DastController.java:183–188` |
| **위반 원칙** | P6 (Trust Boundary) |
| **문제** | `X-Forwarded-For` 헤더를 검증 없이 신뢰. Nginx 없이 직접 접근 시 `consent_ip`(법적 증거)가 위조 가능 |
| **수정 내용** | `X-Real-IP` 우선 사용, 없을 때만 `getRemoteAddr()` 폴백. `X-Forwarded-For` 제거 또는 신뢰 프록시 IP 화이트리스트 체크 추가 |
| **영향 파일** | `DastController.java` (메서드 1개) |
| **예상 작업량** | XS (1시간) |
| **테스트** | 직접 요청 시 실제 IP 저장 확인 / 헤더 조작 시 무시 확인 |

---

## Tier 1 — 단기 수정 (Sprint 7 내)

> 기능은 동작하지만 설계 원칙을 정면으로 위반하는 항목. 방치할수록 연쇄 위반이 늘어난다.

### R-01-A. DastController AI 호출 → Service 레이어로 이동 (P2, P5, P6 동시 해결)

| 항목 | 내용 |
|------|------|
| **파일** | `domain/dast/controller/DastController.java:50–135` |
| **위반 원칙** | P2 (레이어), P5 (Circuit Breaker), P6 (X-Internal-Key 중앙화) |
| **문제** | Controller 내부에 `@PostConstruct` RestClient 생성 + AI Engine 직접 HTTP 호출. 이 파일 하나가 P2·P5·P6를 동시에 위반 |
| **수정 내용** | 1) `AiAgentClient` 인터페이스에 `startDast(DastStartPayload)` 메서드 추가  2) `DefaultAiAgentClient`에 구현 (Circuit Breaker 자동 적용)  3) `DastExecutionService` 또는 신규 `DastOrchestrationService`가 `AiAgentClient` 호출  4) Controller는 Service만 호출 |
| **참조 패턴** | `DefaultAiAgentClient.java` — 동일 방식으로 구현 |
| **영향 파일** | `DastController.java`, `AiAgentClient.java`, `DefaultAiAgentClient.java`, 신규 or 기존 Service |
| **예상 작업량** | M (1일) |
| **테스트** | DAST 시작 E2E + DastController 단위 테스트 (Service mock) |

### R-01-B. ChatService AI 호출 → AiAgentClient 위임 (P5, P6, P7 동시 해결)

| 항목 | 내용 |
|------|------|
| **파일** | `domain/analysis/service/ChatService.java:44–68` |
| **위반 원칙** | P5 (CB 없음), P6 (X-Internal-Key 개별 설정), P7 (SSE 파싱 + 권한 + AI 통신 혼재) |
| **문제** | 생성자에서 `RestClient` 직접 빌드. AI 채팅 스트림 파싱, SSE 릴레이, 세션 권한 검증까지 한 클래스에 혼재 |
| **수정 내용** | 1) `AiChatClient` 인터페이스 신규 생성 (`streamChat(sessionId, request): InputStream`)  2) `DefaultAiChatClient`가 RestClient 관리 + CB 적용  3) `ChatService`는 권한 검증 + SseEmitter 관리만 담당  4) SSE 파싱 로직(`relayLines`)은 `SseRelayHelper`로 추출 |
| **참조 패턴** | `DefaultAiAgentClient.java` (RestClient + CB 구조) |
| **영향 파일** | `ChatService.java`, 신규 `AiChatClient.java`, `DefaultAiChatClient.java` |
| **예상 작업량** | M (1일) |
| **테스트** | `ChatServiceTest.java` 기존 테스트 통과 + 새 Client 단위 테스트 |

### R-01-C. CommitSecretService AI 호출 → AiAgentClient 위임 (P5, P6 해결)

| 항목 | 내용 |
|------|------|
| **파일** | `domain/analysis/service/CommitSecretService.java:45–160` |
| **위반 원칙** | P5 (CB 없음), P6 (X-Internal-Key per-request 중복) |
| **문제** | `@Value` setter로 RestClient 지연 생성. `X-Internal-Key`를 매 요청마다 `.header()` 개별 설정 |
| **수정 내용** | 1) `AiAgentClient`에 `startCommitScan(CommitScanPayload)` 추가  2) `DefaultAiAgentClient`에 구현 (X-Internal-Key 중앙 관리, CB 자동 적용)  3) `CommitSecretService`의 `callAgentScanCommits()` 제거 후 `aiAgentClient.startCommitScan()` 호출 |
| **영향 파일** | `CommitSecretService.java`, `AiAgentClient.java`, `DefaultAiAgentClient.java` |
| **예상 작업량** | S (반나절) |
| **테스트** | `CommitSecretServiceTest.java` 기존 테스트 통과 |

### R-01-D. DastExecutionService CQRS 분리 (P4 해결)

| 항목 | 내용 |
|------|------|
| **파일** | `domain/dast/service/DastExecutionService.java:102–124` |
| **위반 원칙** | P4 (CQRS — Command+Query 혼재) |
| **문제** | `execute()`(Command) 옆에 `getResultsBySessionId()`, `getLatestResultByVulnId()`, `getLatestCompletedByVulnIds()`(Query) 3개 공존 |
| **수정 내용** | 신규 `DastResultQueryService` 생성 후 조회 메서드 3개 이동. `DastController` GET 엔드포인트는 `DastResultQueryService` 참조 |
| **참조 패턴** | `VulnerabilityQueryService.java` — 동일 CQRS 분리 방식 |
| **영향 파일** | `DastExecutionService.java`, 신규 `DastResultQueryService.java`, `DastController.java` |
| **예상 작업량** | S (2시간) |
| **테스트** | `DastExecutionServiceTest.java` + `DastControllerTest.java` 기존 테스트 통과 |

---

## Tier 2 — 중기 수정 (Sprint 8 내)

> 설계 구조 개선. 기능 동작에는 영향 없지만 유지보수·확장성에 직결.

### R-02-A. 크로스 도메인 Repository 주입 → Service Facade 패턴 적용 (P1, P3 해결)

> **가장 광범위한 위반 (8건).** 한 번에 모두 수정하지 않고 도메인별로 단계 분리.

#### 단계 1 — TeamMemberRepository 접근 중앙화

| 항목 | 내용 |
|------|------|
| **영향 파일** | `ChatService`, `CommitSecretService`, `VulnerabilityQueryService`, `AnalysisService` (4개) |
| **수정 내용** | `ProjectService`에 `isMember(UUID projectId, UUID userId): boolean` 추가. 4개 Service에서 `TeamMemberRepository` 직접 주입 제거, `ProjectService.isMember()` 호출로 교체 |
| **예상 작업량** | M (1일) |

#### 단계 2 — ProjectRepository 접근 중앙화

| 항목 | 내용 |
|------|------|
| **영향 파일** | `AnalysisService`, `VulnerabilityService` |
| **수정 내용** | `ProjectService`에 `findOrThrow(UUID projectId): Project` 추가. 직접 주입 제거 |
| **예상 작업량** | S (반나절) |

#### 단계 3 — UserRepository 접근 중앙화

| 항목 | 내용 |
|------|------|
| **영향 파일** | `AnalysisService`, `GitHubApiService`, `OrganizationService` |
| **수정 내용** | `UserService.findOrThrow(userId)`, `UserService.getDecryptedGithubToken(userId)` 추가. 직접 주입 제거 |
| **주의** | `AnalysisService` → `UserService` 의존이 이미 존재. 순환 의존 발생 여부 사전 확인 필요 |
| **예상 작업량** | M (1일) |

#### 단계 4 — PlanRepository 접근 중앙화

| 항목 | 내용 |
|------|------|
| **영향 파일** | `OrganizationService` |
| **수정 내용** | `PlanService.findByNameOrThrow(name)` 추가 또는 Plan을 `global` 패키지로 승격 |
| **예상 작업량** | XS (1시간) |

**전체 완료 기준**: `analysis` 도메인 서비스 중 `project`·`user` 패키지 Repository를 직접 `@Autowired` 하는 코드 0건

### R-02-B. AnalysisService 책임 분리 (P7 해결)

| 항목 | 내용 |
|------|------|
| **파일** | `domain/analysis/service/AnalysisService.java` |
| **위반 원칙** | P7 (의존성 7개, 세션 CRUD + 권한 + GitHub + AI 위임 혼재) |
| **수정 내용** | 1) 권한 검증 로직 → `SessionAccessPolicy` (또는 AOP로) 추출  2) GitHub/local 분기 → `AnalysisSourceResolver.resolve(request)` 추출  3) `AnalysisService`는 세션 상태 전이(`PENDING→RUNNING→CANCELLED`) 만 담당 |
| **선행 조건** | R-02-A 단계 1~3 완료 (TeamMemberRepository, UserRepository 제거) |
| **영향 파일** | `AnalysisService.java`, 신규 `AnalysisSourceResolver.java` |
| **예상 작업량** | L (2일) |
| **테스트** | `AiAgentClientTest.java` + 신규 `AnalysisSourceResolverTest` |

### R-02-C. OrganizationService 분리 (P7 해결)

| 항목 | 내용 |
|------|------|
| **파일** | `domain/organization/service/OrganizationService.java` (350줄) |
| **위반 원칙** | P7 (5개 책임 혼재) |
| **수정 내용** | 1) 초대 관련 메서드(`inviteByEmail`, `acceptInvitation`, `getInvitationInfo`) → `InvitationService`로 이동  2) 권한 검증 헬퍼(`requireAdminOrAbove`, `requireOwner`, `requireMember`) → `OrgAccessPolicy`로 추출  3) `OrganizationService`는 조직 CRUD + 멤버 관리만 |
| **선행 조건** | R-00-A (이메일 발송) 완료 후 진행 |
| **영향 파일** | `OrganizationService.java`, 신규 `InvitationService.java`, `OrgAccessPolicy.java`, `InvitationController.java` 업데이트 |
| **예상 작업량** | M (1일) |

### R-02-D. Resilience4j Circuit Breaker 전체 적용 (P5 해결, TASK-802)

| 항목 | 내용 |
|------|------|
| **현황** | R-01-A~C 완료 후 AI 호출이 모두 `AiAgentClient`로 집중되면 CB 적용이 단순해짐 |
| **수정 내용** | 1) `ResilienceConfig.java` 생성 (`@Bean CircuitBreakerRegistry`)  2) `DefaultAiAgentClient` 수동 CB → Resilience4j `@CircuitBreaker` 교체  3) GitHub API (`GitHubRestClient`, `GitHubOAuthService`), NVD API (`NvdApiClient`)에 `@CircuitBreaker` 적용  4) fallback 메서드 정의 |
| **선행 조건** | R-01-A, R-01-B, R-01-C 완료 (AI 호출 통일) |
| **영향 파일** | `DefaultAiAgentClient.java`, `GitHubRestClient.java`, `NvdApiClient.java`, 신규 `ResilienceConfig.java` |
| **예상 작업량** | M (1일) |
| **테스트** | `AiAgentClientTest.java` CB 동작 + fallback 검증 |

### R-02-E. AnalysisSession.status → SessionStatus enum (P7 보조)

| 항목 | 내용 |
|------|------|
| **파일** | `domain/analysis/entity/AnalysisSession.java`, `AnalysisService.java` |
| **위반 원칙** | P7 (general.md: 매직 문자열 금지) |
| **문제** | `"running"`, `"pending"`, `"interrupted"`, `"cancelled"`, `"completed"` 문자열 리터럴이 여러 파일에 흩어짐 |
| **수정 내용** | `SessionStatus` enum 신규 생성. `AnalysisSession.status` 필드 타입 변경. 관련 Service·Repository 쿼리 수정 |
| **예상 작업량** | M (1일) — DB VARCHAR 컬럼 호환 주의 (Flyway 마이그레이션 불필요, JPA EnumType.STRING 사용) |

---

## Tier 3 — 장기 개선 (Sprint 9 또는 이후)

> 기능·보안에 즉각적 영향은 없으나 코드베이스 일관성을 위해 정리.

### R-03-A. GitHubOAuthService RestTemplate → RestClient 마이그레이션

| 항목 | 내용 |
|------|------|
| **파일** | `domain/auth/service/GitHubOAuthService.java`, `config/AppConfig.java` |
| **수정 내용** | `RestTemplate` → `RestClient`로 교체 후 `AppConfig.restTemplate()` Bean 제거 |
| **예상 작업량** | S (반나절) |

### R-03-B. VulnerabilityService @deprecated 메서드 정리

| 항목 | 내용 |
|------|------|
| **파일** | `domain/analysis/service/VulnerabilityService.java:81–89` |
| **수정 내용** | `VulnerabilityController`가 `VulnerabilityQueryService`를 직접 주입하도록 변경 후 deprecated 위임 메서드 제거 |
| **예상 작업량** | XS (1시간) |

### R-03-C. UserService.deleteMe GDPR 하드 삭제 스케줄러 (TASK-809)

| 항목 | 내용 |
|------|------|
| **파일** | `domain/user/service/UserService.java:67` |
| **수정 내용** | `UserDeletionScheduler` 구현 — soft delete 후 30일 경과 시 연쇄 하드 삭제 (세션, 취약점, 리포트) |
| **예상 작업량** | L (2일) |

---

## 실행 순서 의존성 그래프

```
Tier 0 (즉시)
├── R-00-A  이메일 발송 연동           ← 독립
└── R-00-B  IP Spoofing 수정          ← 독립

         ↓

Tier 1 (Sprint 7)
├── R-01-A  DastController 분리        ← 독립 (AiAgentClient 확장)
├── R-01-B  ChatService 분리           ← 독립 (AiChatClient 신규)
├── R-01-C  CommitSecretService 분리   ← 독립 (AiAgentClient 확장)
└── R-01-D  DastExecutionService CQRS  ← 독립

         ↓ R-01-A~C 완료 후

Tier 2 (Sprint 8)
├── R-02-A  크로스 도메인 Repository   ← 단계별 (1→2→3→4)
│   ├── 단계1: TeamMemberRepository
│   ├── 단계2: ProjectRepository
│   ├── 단계3: UserRepository
│   └── 단계4: PlanRepository
├── R-02-B  AnalysisService 분리       ← R-02-A 단계1~3 완료 후
├── R-02-C  OrganizationService 분리   ← R-00-A 완료 후
├── R-02-D  Resilience4j CB            ← R-01-A~C 완료 후
└── R-02-E  SessionStatus enum         ← 독립

         ↓

Tier 3 (Sprint 9~)
├── R-03-A  RestTemplate 제거
├── R-03-B  deprecated 메서드 제거
└── R-03-C  GDPR 하드 삭제
```

---

## 전체 로드맵 요약표

| ID | 항목 | 위반 원칙 | 타겟 Sprint | 작업량 | 의존 |
|----|------|---------|-----------|--------|------|
| R-00-A | OrganizationService 이메일 연동 | P7 | 즉시 | S | — |
| R-00-B | IP Spoofing 수정 | P6 | 즉시 | XS | — |
| R-01-A | DastController AI 호출 분리 | P2, P5, P6 | Sprint 7 | M | — |
| R-01-B | ChatService AI 호출 분리 | P5, P6, P7 | Sprint 7 | M | — |
| R-01-C | CommitSecretService AI 호출 분리 | P5, P6 | Sprint 7 | S | — |
| R-01-D | DastExecutionService CQRS 분리 | P4 | Sprint 7 | S | — |
| R-02-A | 크로스 도메인 Repository → Facade | P1, P3 | Sprint 8 | L | R-01 완료 |
| R-02-B | AnalysisService 책임 분리 | P7 | Sprint 8 | L | R-02-A |
| R-02-C | OrganizationService 분리 | P7 | Sprint 8 | M | R-00-A |
| R-02-D | Resilience4j Circuit Breaker | P5 | Sprint 8 | M | R-01 완료 |
| R-02-E | SessionStatus enum | P7 | Sprint 8 | M | — |
| R-03-A | RestTemplate → RestClient | — | Sprint 9+ | S | — |
| R-03-B | deprecated 메서드 제거 | P2 | Sprint 9+ | XS | — |
| R-03-C | GDPR 하드 삭제 스케줄러 | — | Sprint 9+ | L | — |

**작업량 기준**: XS = 1시간 이하 / S = 반나절 / M = 1일 / L = 2일

---

## 리팩토링 완료 시 개선되는 원칙 현황

| 원칙 | 현재 | 완료 후 |
|------|------|--------|
| P1 Modular Monolith | ❌ 8건 위반 | ✅ 0건 (R-02-A) |
| P2 Layered Architecture | ❌ 1건 위반 | ✅ 0건 (R-01-A) |
| P3 DDD Bounded Context | ❌ 8건 위반 | ✅ 0건 (R-02-A) |
| P4 CQRS | ⚠️ 부분 | ✅ 준수 (R-01-D) |
| P5 Circuit Breaker | ⚠️ 1/4만 적용 | ✅ 전체 적용 (R-01-A~C + R-02-D) |
| P6 Trust Boundary | ⚠️ 부분 | ✅ 중앙화 (R-00-B + R-01-A~C) |
| P7 SRP | ❌ 다수 위반 | ✅ 핵심 서비스 정리 (R-01-B + R-02-B~C + R-02-E) |

---

*참조: `docs/refactoring/20260516_stage3.md` | `docs/07_SPRINT_BACKLOG_V3.md` | `docs/05_ARCHITECTURE_PHILOSOPHY.md`*
