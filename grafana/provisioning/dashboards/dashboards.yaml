apiVersion: 1

providers:
  - name: "SecureAI Dashboards"
    orgId: 1
    folder: "SecureAI"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
